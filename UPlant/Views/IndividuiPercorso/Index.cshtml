@model IEnumerable<UPlant.Models.DB.IndividuiPercorso>
@inject LanguageService language
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @language.Getkey("Global_Progressive")
            </th>
            <th>
                @Html.DisplayNameFor(model => model.percorsoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.individuoNavigation.progressivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.percorsoNavigation.autore)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.id">@language.Getkey("Global_Edit")</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
