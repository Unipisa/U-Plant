@model UPlant.Models.DB.UserRole

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserRole</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserFK" class="control-label"></label>
                <select asp-for="UserFK" class ="form-control" asp-items="ViewBag.UserFK"></select>
            </div>
            <div class="form-group">
                <label asp-for="RoleFK" class="control-label"></label>
                <select asp-for="RoleFK" class ="form-control" asp-items="ViewBag.RoleFK"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
