@model IEnumerable<UPlant.Models.DB.TipoInterventiAlberi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.descrizione)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descrizione_en)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.organizzazione)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ordinamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.descrizione)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descrizione_en)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.organizzazione)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ordinamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
